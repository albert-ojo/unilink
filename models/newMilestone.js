class NewMilestone {
  constructor(id, name, done, userId, stage, description, dueDate) {
    this.id = id;
    this.name = name;
    this.done = done;
    this.userId = userId;
    this.stage = stage;
    this.description = description;
    this.dueDate = dueDate;
  }
}

export default NewMilestone;
