class CurrentRoles {
  constructor(id, role, name, projectId, userId) {
    this.id = id;
    this.role = role;
    this.name = name;
    this.projectId = projectId;
    this.userId = userId;
  }
}

export default CurrentRoles;
